# Drop-in for:
#   /etc/systemd/system/healtharchive-worker.service.d/override.conf
#
# Goal: keep the public API responsive during annual crawl campaigns by
# lowering worker CPU/IO priority (single VPS; no "safe window").

[Service]
Nice=10
IOSchedulingClass=best-effort
IOSchedulingPriority=6
